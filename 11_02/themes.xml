<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme._11_02" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:windowFullscreen">true</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="heading">
        <item name="android:textColor">@color/primaryColor</item>
        <item name="android:textSize">32sp</item>
        <item name="android:textAlignment">center</item>
        <item name="android:layout_marginBottom">30dp</item>
    </style>

    <style name="label">
        <item name="android:textColor">@color/labelColor</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textAlignment">textStart</item>
        <item name="android:layout_marginBottom">15dp</item>
    </style>

    <style name="reported" parent="label">
        <item name="android:textColor">@color/primaryColor</item>
        <item name="android:layout_marginStart">20dp</item>
    </style>

    <style name="input">
        <item name="android:textColor">@color/primaryColor</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textAlignment">textStart</item>
        <item name="android:layout_marginBottom">10dp</item>
    </style>

    <style name="button">
        <item name="android:textColor">@color/white</item>
        <item name="android:backgroundTint">@color/primaryColor</item>
    </style>

    <style name="Theme._11_02" parent="Base.Theme._11_02" />
</resources>